name: Scrape HR Jobs

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Scrape HR jobs
        env:
          SEARCH_TERM: "Human Resources"
          LOCATION: "98589"
          DISTANCE: "200"
          SITES: "indeed,linkedin"
          RESULTS_WANTED: "50"
          HOURS_OLD: "24"
          LOCAL_ONLY: "true"
          OUTPUT_FILE: "hr-jobs.csv"
          INCLUDE_TITLES: "HR,Human Resources,People Operations,People Partner,HRBP"
          EXCLUDE_TITLES: "HRIS Developer,Software Engineer,Nurse,RN,Medical,CNA"
        run: python src/scraper/scrape.py

      - name: Commit data
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/
          git diff --staged --quiet || git commit -m "Update HR jobs data $(date -u +%Y-%m-%d)"
          git push

      - name: Update README with daily report
        run: python src/update_readme.py

      - name: Commit README
        run: |
          git add README.md
          git diff --staged --quiet || git commit -m "Update daily job report"
          git push
