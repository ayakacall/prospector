name: Scrape Japanese Jobs (Remote)

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        env:
          SEARCH_TERM: "japanese"
          LOCATION: "United States"
          SITES: "indeed,glassdoor,linkedin"
          RESULTS_WANTED: "50"
          HOURS_OLD: "24"
          IS_REMOTE: "true"
          REMOTE_ONLY: "true"
          OUTPUT_FILE: "japanese-jobs.csv"
          INCLUDE_TITLES: "Japanese,Bilingual,Translator,Interpreter,Japan,Nihongo"
          EXCLUDE_TITLES: "Japanese Restaurant,Sushi,Chef,Cook"
        run: python src/scraper/scrape.py

      - name: Commit data
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/
          git diff --staged --quiet || git commit -m "Update Japanese jobs data $(date -u +%Y-%m-%d)"
          git push

      - name: Update README with daily report
        run: python src/update_readme.py

      - name: Commit README
        run: |
          git add README.md
          git diff --staged --quiet || git commit -m "Update daily job report"
          git push
